# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package `src/swin_classification_pipeline/` (the in-notebook validator, the in-kernel finetuner, the artifact contract and the `validate_inputs` / `majority_class_baseline` / `evaluation_report` helpers extracted from the previous notebook revision), the offline unit tests, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/swin_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit is recorded in `docs/release-verification.md`; the previous (non-standalone) notebook revision's local pre-flight runs are kept there as history and do not carry over to the regenerated notebook.
